---
name: doc-sync
description: 機能を追加・変更・修正したら、同じ作業内で関連ドキュメントも更新する（コードとドキュメントの乖離を作らない）
globs:
  - "Assets/**/*.cs"
  - "Assets/**/*.unity"
  - "Assets/**/*.prefab"
  - "Assets/Editor/**"
  - "tools/**"
---

# ドキュメント同期の癖（必須）

**コードや機能を変えたら、その変更を反映したドキュメント更新を「同じ作業・同じコミット」で行う。**
「コードだけ直してドキュメントが古いまま」を作らない（ユーザーから複数回指摘済み → 恒久ルール化）。

## 発動タイミング

機能を **追加 / 変更 / 削除 / 修正 / 挙動を変える** 操作をしたら、コミット前に次を自問して該当を更新する：

- 新しいコンポーネント・メニュー・起動フラグ・パラメータを足した／消した／意味を変えた
- セットアップ手順・ビルド方法・起動方法・運用 runbook が変わった
- 既知の罠・前提・制約が増えた／解消した
- フェーズ／実装状況のステータスが進んだ

## 更新先の対応表（迷ったらここを見る）

| 変えたもの | 反映先 |
|---|---|
| プロジェクト全体の構成・ビルド/起動方法・実装済み機能・ディレクトリ | [README.md](../../README.md) |
| 設計判断・要件・調査設計 | `docs/`（例: `docs/table-duo/*`） |
| 実装状況・runbook・罠・残課題 | 該当の `.claude/plans/<date>_<slug>.md` |
| 次セッションが最初に知るべき現状・落とし穴 | `.claude/memory/*`（`MEMORY.md` 索引も更新） |
| 作業規約・再発防止ルール | `.claude/rules/*`（このファイル群） |
| ハーネス全体の役割（CLAUDE.md が指す導線） | [CLAUDE.md](../../CLAUDE.md) |

## やり方

1. コード変更とドキュメント更新を **1 コミットにまとめる**（差分が一目で対応する）
2. 数行で良い。「何を・どう変えたか」と「他シュビーが踏みそうな罠」を端的に
3. 古い記述は消すか「廃止」マークを付ける（残すと誤誘導になる）
4. README は顔。構成・ビルド・起動が変わったら必ず見直す（[git-workflow.md](git-workflow.md) の「ドキュメント同期チェック」も参照）

## 例（TableDuo）

- リモート手をカプセル→Meta 白メッシュに変えた → memory `table_duo_study_status` の該当行 + plan に追記 + RemoteHandMeshProvider を記載
- ビルド方式を BuildVariants 2 アプリに統一した → README「ビルド & デプロイ」を書き換え
